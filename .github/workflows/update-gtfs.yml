name: MAJ GTFS (Automatique)

on:
  push:
    branches: [main]
  schedule:
    - cron: '15 2 * * *'

jobs:
  maj-gtfs:
    runs-on: ubuntu-latest
    steps:
      - name: 🛒 Checkout du repo
        uses: actions/checkout@v4

      - name: 🟩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: ⬇️ Installation des dépendances
        run: |
          npm install csv-parse unzipper node-fetch

      - name: ⚡ Exécution du script update-gtfs.js
        run: node scripts/update-gtfs.js

      - name: 💾 Commit & Push fichiers statiques
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add static/gtfs-stops.json static/gtfs-firstlast.json last-update.txt
          git commit -m "🤖 Maj automatique GTFS [bot]" || echo "No changes to commit"
          git push
